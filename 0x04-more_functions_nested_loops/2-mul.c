#include "main.h"
/**
 * mul - Check a function that mutiplies two integers
 * @x: The first number is multiply
 * @y: The second number is multiply with first number
 * Return: The result of multiplication between x paran and y paran
 */

int mul(int x, int y)
{
return (x * y);
}
